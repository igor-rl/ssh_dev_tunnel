# Adiciona as mudanças e criar nova versao do pacote

```cmd
git add . && git commit -m "v3.7.2: fix: install and unisntall | issue: pass is not saving"
git push origin main && git tag v3.7.2 && git push origin v3.7.2
```