# Adiciona as mudanças e criar nova versao do pacote

```cmd
git add . && git commit -m "v3.7.8: fix: issue: root user is not enable on cursor iID"
git push origin main && git tag v3.7.8 && git push origin v3.7.8
```